{{>message}}
<!-- Flex container for sidebar and main content -->
    <div class="flex min-h-screen bg-gray-300">
        <!-- Sidebar -->
        <aside>
            {{> sidebar}}
        </aside>
        
        <!-- Main content -->
        <div class="flex flex-1 flex-col items-center justify-center p-5">
            {{#if location}}
                <div class="card bg-white shadow-lg p-6 max-w-md w-full mb-5">
                    <h1 class="card-title text-xl mb-4 text-gray-700">Location</h1>
                    {{#if location.minhardiness}}<h2 class="text-gray-700"><b>Min Hardiness: </b>{{location.minhardiness}}</h2>{{/if}}

                    {{#if location.maxhardiness}}<h2 class="text-gray-700"><b>Max Hardiness: </b>{{location.maxhardiness}}</h2>{{/if}}

                    {{#if location.sunlight}}<h2 class="text-gray-700"><b>Sunlight: </b>{{location.sunlight}}</h2>{{/if}}

                    {{#if location.watering}}<h2 class="text-gray-700"><b>Watering: </b>{{location.watering}}</h2>{{/if}}
                </div>
            {{/if}}
            <div class="w-full max-w-md p-6 bg-white rounded-lg shadow-md m-5">
                <h1 class="card-title text-xl mb-4 text-gray-700">Location Settings</h1>
                <form action="/location/add" method="POST">
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700" for="minHardiness">Min Hardiness</label>
                        <input type="text" id="minHardiness" name="minHardiness" placeholder="Enter an integer 1-12" class="w-full mt-1 input input-bordered" />
                    </div>
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700" for="maxHardiness">Max Hardiness</label>
                        <input type="text" id="maxHardiness" name="maxHardiness" placeholder="Enter an integer 1-12" class="w-full mt-1 input input-bordered" />
                    </div>
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700" for="sunlight">Sunlight</label>
                        <select id="sunlight" name="sunlight" class="w-full mt-1 input input-bordered">
                            <option value="">Select an option:</option>
                            <option value="Full Shade">Full Shade</option>
                            <option value="Part Shade">Part Shade</option>
                            <option value="Full Sun">Full Sun</option>
                        </select>
                    </div>
                    <div class="mb-6">
                        <label class="block text-sm font-medium text-gray-700" for="watering">Watering</label>
                        <select id="watering" name="watering" class="w-full mt-1 input input-bordered">
                            <option value="">Select an option:</option>
                            <option value="Minimum">Minimum</option>
                            <option value="Average">Average</option>
                            <option value="Frequent">Frequent</option>
                        </select>
                    </div>
                    <button type="submit" class="w-full btn bg-green-300 text-gray-700">Confirm</button>
                </form>
            </div>
        </div>
    </div>
